---
title: Valibot SEO Title
description: Valibot SEO description — written in frontmatter, surfaced by the valibotSeo trait.
date: '2026-04-02'
dateEnd: '2026-04-03'
---

This paragraph is the **markdown body** — parsed and rendered by `<ContentRenderer>` in Vue.

The fields above (`title`, `description`, `date`, `dateEnd`) come from the YAML frontmatter. They are validated and exposed by the `valibotSeo` and `valibotDates` traits defined with **Valibot** schemas.
